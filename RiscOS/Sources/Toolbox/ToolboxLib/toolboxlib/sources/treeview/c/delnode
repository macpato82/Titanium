/************************************************************************
 * © Acorn Computers Ltd, 1994.                                         *
 *                                                                      *
 * It may be freely used to create executable images for saleable       *
 * products but cannot be sold in source form or as an object library   *
 * without the prior written consent of Acorn Computers Ltd.            *
 *                                                                      *
 * If this file is re-distributed (even if modified) it should retain   *
 * this copyright notice.                                               *
 *                                                                      *
 ************************************************************************/
/* Modernised 2026 (formatting/structure only; behaviour unchanged). */


/*
 * Name        : delnode.c
 * Purpose     : Veneer for method TreeView_DeleteNode
 * Description : Deletes the current node and any descendants it has
 */


#include "kernel.h"
#include "toolbox.h"
#include "gadgets.h"
#include "treeview.h"




/*
 * Name        : treeview_delete_node
 * Description : Deletes the current node and any descendants it has
 * In          : unsigned int flags
 *               ObjectId window
 *               ComponentId gadget
 * Out         : int *new_id
 * Returns     : pointer to error block
 */

extern _kernel_oserror *treeview_delete_node ( unsigned int flags,
                                               ObjectId window,
                                               ComponentId gadget,
                                               int *new_id
                                             )
{
  _kernel_swi_regs r;
  _kernel_oserror *e;

  r.r[0] = flags;
  r.r[1] = (int) window;
  r.r[2] = TreeView_DeleteNode;
  r.r[3] = (int) gadget;
  if ((e = _kernel_swi(Toolbox_ObjectMiscOp,&r,&r)) == NULL)
  {
    if (new_id != NULL) *new_id = (int) r.r[0];
  }

  return (e);
}

